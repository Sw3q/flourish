/**
 * Global configuration for the Flourish Fund application.
 */
export const CONFIG = {
    /**
     * Set to true to bypass Supabase authentication and approval logic.
     * Useful for local development, demoing, and UI exploration.
     */
    BYPASS_AUTH: false,
};
